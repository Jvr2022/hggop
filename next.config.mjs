/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // Allow local static images; adjust domains if using external images
    remotePatterns: [],
  },
};

export default nextConfig;
